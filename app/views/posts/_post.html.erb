<p><%= image_tag post.avatar.url(:thumb) %></p>
<h2><%= post.title %></h2>
<p class="text-muted", id="post-date"><%= post.date %></p>
<p><%=raw post.body.truncate(200) %></p>
<%= link_to "Read More", admin_post_path(post), class:"btn btn-lg btn-info", id:"read-more" %>
